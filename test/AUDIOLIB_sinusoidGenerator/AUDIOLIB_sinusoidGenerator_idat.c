// Copyright (C) 2026 Texas Instruments Incorporated
//
// SPDX-License-Identifier: Apache-2.0

#include "AUDIOLIB_sinusoidGenerator_idat.h"
/*This file is autogenerated*/
/*Please do not edit*/

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 1))
#include "test_data/staticRefCase1.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 2))
#include "test_data/staticRefCase2.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 3))
#include "test_data/staticRefCase3.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 4))
#include "test_data/staticRefCase4.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 5))
#include "test_data/staticRefCase5.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 6))
#include "test_data/staticRefCase6.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 7))
#include "test_data/staticRefCase7.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 8))
#include "test_data/staticRefCase8.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 9))
#include "test_data/staticRefCase9.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 10))
#include "test_data/staticRefCase10.h"
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 11))
#include "test_data/staticRefCase11.h"
#endif

static AUDIOLIB_sinusoidGenerator_testParams_t testParams[] = {
#if (defined(ALL_TEST_CASES) || (TEST_CASE == 1))
    {
        STATIC,                    // Test pattern
        staticRefOutCase1,         // Output buffer
        256,                       // numSamples
        2500.0,                    // startFrequency
        2600.0,                    // targetFrequency
        330.0,                     // startPhase
        5.0,                       // smoothingTime
        48000,                     // samplingRate
        1,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        1,                         // test ID
        4,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 2))
    {
        STATIC,                    // Test pattern
        staticRefOutCase2,         // Output buffer
        256,                       // numSamples
        2500.0,                    // startFrequency
        2600.0,                    // targetFrequency
        330.0,                     // startPhase
        5.0,                       // smoothingTime
        48000,                     // samplingRate
        0,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        2,                         // test ID
        4,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 3))
    {
        STATIC,                    // Test pattern
        staticRefOutCase3,         // Output buffer
        254,                       // numSamples
        2500.0,                    // startFrequency
        2600.0,                    // targetFrequency
        330.0,                     // startPhase
        5.0,                       // smoothingTime
        48000,                     // samplingRate
        1,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        3,                         // test ID
        4,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 4))
    {
        STATIC,                    // Test pattern
        staticRefOutCase4,         // Output buffer
        7,                         // numSamples
        500.0,                     // startFrequency
        3000.0,                    // targetFrequency
        240.0,                     // startPhase
        5.0,                       // smoothingTime
        48000,                     // samplingRate
        1,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        4,                         // test ID
        7,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 5))
    {
        STATIC,                    // Test pattern
        staticRefOutCase5,         // Output buffer
        512,                       // numSamples
        3000.0,                    // startFrequency
        2000.0,                    // targetFrequency
        330.0,                     // startPhase
        10.0,                      // smoothingTime
        48000,                     // samplingRate
        1,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        5,                         // test ID
        3,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 6))
    {
        STATIC,                    // Test pattern
        staticRefOutCase6,         // Output buffer
        384,                       // numSamples
        3000.0,                    // startFrequency
        3000.0,                    // targetFrequency
        330.0,                     // startPhase
        10.0,                      // smoothingTime
        48000,                     // samplingRate
        0,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        6,                         // test ID
        3,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 7))
    {
        STATIC,                    // Test pattern
        staticRefOutCase7,         // Output buffer
        67,                        // numSamples
        3000.0,                    // startFrequency
        3000.0,                    // targetFrequency
        330.0,                     // startPhase
        0.0,                       // smoothingTime
        48000,                     // samplingRate
        1,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        7,                         // test ID
        5,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 8))
    {
        STATIC,                    // Test pattern
        staticRefOutCase8,         // Output buffer
        67,                        // numSamples
        3000.0,                    // startFrequency
        3000.0,                    // targetFrequency
        330.0,                     // startPhase
        0.0,                       // smoothingTime
        48000,                     // samplingRate
        0,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        8,                         // test ID
        5,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 9))
    {
        STATIC,                    // Test pattern
        staticRefOutCase9,         // Output buffer
        254,                       // numSamples
        2500.0,                    // startFrequency
        2600.0,                    // targetFrequency
        330.0,                     // startPhase
        5.0,                       // smoothingTime
        48000,                     // samplingRate
        0,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        9,                         // test ID
        4,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 10))
    {
        STATIC,                    // Test pattern
        staticRefOutCase10,        // Output buffer
        17,                        // numSamples
        1000.0,                    // startFrequency
        3000.0,                    // targetFrequency
        330.0,                     // startPhase
        0.0,                       // smoothingTime
        48000,                     // samplingRate
        1,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        10,                        // test ID
        5,                         // numExecReps
    },
#endif

#if (defined(ALL_TEST_CASES) || (TEST_CASE == 11))
    {
        STATIC,                    // Test pattern
        staticRefOutCase11,        // Output buffer
        67,                        // numSamples
        3000.0,                    // startFrequency
        3000.0,                    // targetFrequency
        61000000.0,                // startPhase
        0.0,                       // smoothingTime
        48000,                     // samplingRate
        0,                         // executionMode (1=VECTOR, 0=SCALAR)
        AUDIOLIB_FLOAT32,          // precision
        AUDIOLIB_TEST_OUTPUT_MSMC, // Output data location
        1,                         // number of reps.
        11,                        // test ID
        5,                         // numExecReps
    },
#endif

};

/*
 *  Sends the test parameter structure and number of tests
 */
void AUDIOLIB_sinusoidGenerator_getTestParams(AUDIOLIB_sinusoidGenerator_testParams_t** params, int32_t* numTests)
{
   *params   = testParams;
   *numTests = sizeof(testParams) / sizeof(AUDIOLIB_sinusoidGenerator_testParams_t);
}
