/*
 *  Sends the test parameter structure and number of tests
 */
void AUDIOLIB_nlms_getTestParams (
    AUDIOLIB_nlms_testParams_t **params,
    int32_t *                                 numTests)
{
   *params = testParams;
   *numTests =
       sizeof (testParams) / sizeof (AUDIOLIB_nlms_testParams_t);
}
