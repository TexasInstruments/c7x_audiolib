/*
 *  Sends the test parameter structure and number of tests
 */
void AUDIOLIB_concat_getTestParams (
    AUDIOLIB_concat_testParams_t **params,
    int32_t *                                 numTests)
{
   *params = testParams;
   *numTests =
       sizeof (testParams) / sizeof (AUDIOLIB_concat_testParams_t);
}
