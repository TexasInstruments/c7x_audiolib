/*
 *  Sends the test parameter structure and number of tests
 */
void AUDIOLIB_whiteNoiseGenerator_getTestParams (
    AUDIOLIB_whiteNoiseGenerator_testParams_t **params,
    int32_t *                                 numTests)
{
   *params = testParams;
   *numTests =
       sizeof (testParams) / sizeof (AUDIOLIB_whiteNoiseGenerator_testParams_t);
}
