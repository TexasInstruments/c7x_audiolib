/*
 *  Sends the test parameter structure and number of tests
 */
void AUDIOLIB_gain_getTestParams (
    AUDIOLIB_gain_testParams_t **params,
    int32_t *                                 numTests)
{
   *params = testParams;
   *numTests =
       sizeof (testParams) / sizeof (AUDIOLIB_gain_testParams_t);
}
